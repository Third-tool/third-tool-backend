# [<type>] {제목} — {version 또는 식별자}

> 본 문서는 `sdd-lite` 빈 스켈레톤. 양식 가이드는 `./sdd-lite.md` 참조. 작성 시점에 본 인용 박스 제거.
>
> **본 문서의 역할**: {왜 이 문서가 필요한가 — 한 단락}
> **본 양식**: sdd-lite
> **단방향/양방향**: {예: 백엔드 → 프론트엔드 단방향}

작성 시점: {YYYY-MM-DD} · 기준 브랜치/태그: {feat/... 또는 v0.0.1}

---

## 1. Context — 왜 이 문서가 필요한가
{인계 트리거 / 마일스톤 위치 / 검증할 정합성 / 다음 행동}

---

## 2. 스냅샷

### 2.1 영향 항목
| 항목 | 카테고리 | 상태 | 영향 | 행동 |
| --- | --- | --- | --- | --- |
| {Story 번호 / 변경명} | {BE 코드/관측성/인프라} | {✅ 머지 / 진행 중 / 계획} | {영향 1줄} | {섹션 N에서 처리} |

### 2.2 진실 소스
| 영역 | 진실 소스 경로 |
| --- | --- |
| API 시그니처 | {경로} |
| Request/Response DTO | {경로} |
| ErrorCode enum | {경로} |

---

## 3. 인벤토리 (해당 시 — 미사용이면 섹션 자체 제거)

### 3.1 호출 가능 (화이트리스트)
| METHOD | PATH | Request DTO | Response DTO | 인증 | 비고 |
| --- | --- | --- | --- | --- | --- |
| ... |

### 3.2 호출 금지 (미공개)
> - {Port명} ({Story 번호}) — 사유 / 다음 버전 노출 예정

---

## 4. 계약 (해당 시 — 미사용이면 섹션 자체 제거)

### 4.1 성공 응답
{JSON 예시}

### 4.2 에러 응답
{JSON 예시 + ErrorCode 분기표}

### 4.3 헤더 / 인증 매체
{표 또는 박스}

---

## 5. 작업 체크리스트

형식: `[ ] {path} — {what} — {why} — {check}`

### 5.1 {큰 작업명}
- [ ] {경로} — {무엇} — {왜} — {확인 방법}
- [ ] {경로} — {무엇} — {왜} — {확인 방법}

### 5.2 {다른 큰 작업명}
- [ ] ...

---

## 6. 검증 절차 / Runbook

### 6.1 사전 부팅 신호
```bash
{curl 또는 명령}
# 기대: {응답}
```

### 6.2 N 시나리오
#### 시나리오 1 — {제목}
```bash
{curl 명령}
```
**기대 응답**:
```http
{status / headers / body}
```

### 6.3 통과 신호 (모두 OK)
- [ ] 6.1 사전 신호 OK
- [ ] 6.2 N 시나리오 모두 통과
- [ ] {추가 신호}

---

## 7. Open Questions / 다음 버전 인계

### 7.1 {미결 항목}
{한 단락}

---

## 8. 자가 점검 (작성자가 통과 확인)

- [ ] 인계 완결성 — 받는 측이 본 문서만으로 6.2 시나리오 실행 가능
- [ ] 사실 정합성 — 인용된 endpoint·DTO·ErrorCode가 실제 코드와 일치
- [ ] 작업 실행성 — 섹션 5의 모든 항목이 `path/what/why/check` 4축 정합
- [ ] 다음 버전 연결 — 섹션 7이 다음 문서의 자연스러운 시작점

---

## 참고 링크

- 관련 마일스톤 / Product / Epic / Story 링크
