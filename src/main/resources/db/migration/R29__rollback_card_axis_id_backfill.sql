-- =============================================================
-- R29. Card.axisId 백필 롤백
-- Story-LT-E4-S4-2 rollback
--
-- 주의: 백필된 값은 deck.axis_id 스냅샷이므로 롤백 시 NULL로 되돌려 다시 간접 참조 상태 회귀.
-- =============================================================

UPDATE card
   SET axis_id = NULL;
