-- =============================================================
-- R26. Card.createdMode 백필 롤백
-- Story-CARD-E3-S3-1 rollback
--
-- 주의: 백필된 값은 원본 정보가 아니므로 롤백 시 모든 값을 NULL로 되돌린다.
-- =============================================================

UPDATE card
   SET created_mode = NULL;
