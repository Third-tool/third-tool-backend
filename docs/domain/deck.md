# Deck 도메인 모델

> 학습 카드를 주제 단위로 묶고, 계층 구조와 공개 라이브러리 등록을 관리하는 BC.

> 이 문서는 [`도메인모델.md`](도메인모델.md#deck--학습-카드를-주제-단위로-묶어-관리하는-컨테이너)의 Deck 섹션을 도메인별 파일로 분리한 신규 위치입니다. 작성 규칙은 [`_rules.md`](_rules.md) 참조.

---

## 구성 요소

| 구분 | 클래스 | 역할 |
|---|---|---|
| Aggregate Root | `Deck` | 카드 컨테이너. 계층 구조와 공개 상태 관리 |
| Enum | `DeckMode` | 덱 모드 분류값 |

---

## Deck (Aggregate Root)

> 카드 목록 + 하위 덱 + 소유 사용자 + 공개 여부를 한 단위로 묶는 학습 컨테이너.

### 설명
→ [`도메인모델.md` Deck 섹션](도메인모델.md#deck--학습-카드를-주제-단위로-묶어-관리하는-컨테이너) 참조.

### 속성
→ 위 링크 참조.

### 행위
→ 위 링크 참조.

### 규칙
→ 위 링크 참조.

### 검증
→ 위 링크 참조.

---

## DeckMode (Enum)

> 덱의 학습/공유 모드를 표현하는 분류값.

### 값
_TBD_ — `Deck/domain/model/DeckMode.java` 기준으로 채워주세요.

### 규칙
_TBD_

---

## 책임 분리

→ [`도메인모델.md` Deck Aggregate 책임 분리 섹션](도메인모델.md#deck-aggregate) 참조.
